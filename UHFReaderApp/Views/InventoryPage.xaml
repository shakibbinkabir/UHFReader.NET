<Page
    x:Class="UHFReaderApp.Views.InventoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Page Title -->
        <TextBlock Grid.Row="0" 
                  Text="Tag Inventory" 
                  Style="{ThemeResource HeaderTextBlockStyle}" 
                  Margin="0,0,0,20"/>
        
        <!-- Control Panel -->
        <Border Grid.Row="1" 
               Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
               CornerRadius="8" 
               Padding="15" 
               Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="15">
                    <Button x:Name="StartScanButton" 
                           Content="Start Scanning" 
                           Click="StartScanButton_Click"
                           Style="{ThemeResource AccentButtonStyle}"/>
                    <Button x:Name="ClearButton" 
                           Content="Clear List" 
                           Click="ClearButton_Click"/>
                    <Button x:Name="ExportButton" 
                           Content="Export CSV" 
                           Click="ExportButton_Click"/>
                </StackPanel>
                
                <TextBlock Grid.Column="1" 
                          x:Name="StatusText" 
                          Text="Ready to scan" 
                          VerticalAlignment="Center" 
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </Grid>
        </Border>
        
        <!-- Tags List -->
        <Border Grid.Row="2" 
               Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
               CornerRadius="8">
            <ListView x:Name="TagsListView" 
                     SelectionMode="Single">
                <ListView.Header>
                    <Border Background="{ThemeResource AccentFillColorDefaultBrush}" 
                           CornerRadius="8,8,0,0" 
                           Padding="15,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" 
                                     Text="EPC" 
                                     FontWeight="SemiBold" 
                                     Foreground="White"/>
                            <TextBlock Grid.Column="1" 
                                     Text="First Seen" 
                                     FontWeight="SemiBold" 
                                     Foreground="White"/>
                            <TextBlock Grid.Column="2" 
                                     Text="Last Seen" 
                                     FontWeight="SemiBold" 
                                     Foreground="White"/>
                            <TextBlock Grid.Column="3" 
                                     Text="Reads" 
                                     FontWeight="SemiBold" 
                                     Foreground="White" 
                                     HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="4" 
                                     Text="Length" 
                                     FontWeight="SemiBold" 
                                     Foreground="White" 
                                     HorizontalAlignment="Center"/>
                        </Grid>
                    </Border>
                </ListView.Header>
                
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="15,10" 
                             Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" 
                                     Text="{Binding EPC}" 
                                     FontFamily="Consolas"
                                     TextWrapping="NoWrap"/>
                            <TextBlock Grid.Column="1" 
                                     Text="{Binding FirstSeenFormatted}"/>
                            <TextBlock Grid.Column="2" 
                                     Text="{Binding LastSeenFormatted}"/>
                            <TextBlock Grid.Column="3" 
                                     Text="{Binding ReadCount}" 
                                     HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="4" 
                                     Text="{Binding Length}" 
                                     HorizontalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
                
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Border>
        
        <!-- Info Panel -->
        <Border Grid.Row="3" 
               Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
               CornerRadius="8" 
               Padding="15" 
               Margin="0,15,0,0">
            <TextBlock TextWrapping="Wrap" 
                      Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                Click "Start Scanning" to begin continuous tag detection. Tags will be added to the list as they're found.
                The scanner will continue until you click "Stop Scanning". Use "Export CSV" to save the tag list.
            </TextBlock>
        </Border>
    </Grid>
</Page>